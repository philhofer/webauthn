# WebAuthn Library

This is a fork of github.com/go-webauthn/webauthn, which in turn is a community fork of github.com/duo-labs/webauthn.

This fork exists mostly to provide a feature-reduced and dependency-reduced version of the upstream webauthn library.
